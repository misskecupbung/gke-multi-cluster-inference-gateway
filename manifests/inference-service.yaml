apiVersion: v1
kind: Service
metadata:
  name: inference-backend
  labels:
    app: inference-backend
spec:
  selector:
    app: inference-backend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
